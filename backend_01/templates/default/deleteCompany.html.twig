{% extends 'base.html.twig' %}

{% block header %}
    {{ include('partial/header.html.twig') }}
{% endblock %}

{% block body %}
    <p>¿Seguro que quiere eliminar la empresa de la base de datos?</p>
    <a href="{{ path('default_index_eliminar_empresa') }}" class="btn btn-success">Cancelar</a>
    <a href="{{ path('default_index_eliminar_empresa', { id: id }) }}" class="btn btn-danger">Eliminar</a>
{% endblock %}